<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Flutter writes this into the debug and profile manifests for you and
         *not* into this one, so a release build had no network at all: the
         box showed "no connection" while `adb shell` reached the very same
         address from the very same device, because the shell has the
         permission and the app did not. Every debug run worked, which is
         what made it look like a server problem for as long as it did. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- `usesCleartextTraffic` below: Android has blocked plain http by default
         since API 28, and this app needs it for two separate reasons.

         The streams. The ad-stitching host presents a chain ending in the old
         Go Daddy Class 2 root signed with SHA-1, which Android rejects outright
         — which is why the API hands out a `plain_url` beside the https one and
         says so in `modules/tv/schemas.py`. Without this, the one address that
         works on a box is the one the box refuses to open.

         And the API in development, which is `http://127.0.0.1:8000` reached
         over `adb reverse`. Without this the box shows "no network connection"
         while the server answers every request beside it. -->
    <application
        android:label="Сеанс"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <!-- Written out in full on purpose, though the namespace now matches.
             The Flutter template had left a second, empty `MainActivity` in the
             namespace package, and the short `.MainActivity` quietly resolved to
             that one: no channel was ever registered, `box.installer()` threw,
             `main()` died before `runApp`, and the box came up black. The stub
             is deleted; naming the class outright is what stops it coming back
             the next time a template is regenerated. -->
        <activity
            android:name="tv.seans.launcher.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
