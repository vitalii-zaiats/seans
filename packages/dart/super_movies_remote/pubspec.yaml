name: super_movies_remote
description: Driving a box from a phone, and being the box — commands over HTTP, state over server-sent events.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  http: ^1.6.0
  meta: ^1.19.0
  super_movies_api: ^0.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
