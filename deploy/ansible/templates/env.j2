# Written by `deploy/ansible/deploy.yml`. Edits here are overwritten on the next
# deploy — change the workflow's variables instead.
DOMAIN={{ domain }}
ACME_EMAIL={{ acme_email }}
POSTGRES_PASSWORD={{ postgres_password }}
REGISTRY={{ registry }}
TAG={{ tag }}
API_SELF_UPDATE_URL={{ self_update_url | default('', true) }}
