"""What this build is allowed to do, and whether it should be a newer one.

No tables. Both answers are functions of `(platform, vendor, version)` and come
out of configuration — a switch you have to redeploy to flip is still a switch,
and a table nobody has needed yet is a migration nobody has needed yet.
"""
