## 0.1.0

- `start()` — `POST /init` in all three account modes.
- Identity: `becomeGuest`, `claim`, `register`, `signIn`, `signOut`, `me`,
  `rename`, `forget`, with the session token managed for you.
- `Transport` seam with a `package:http` default.
