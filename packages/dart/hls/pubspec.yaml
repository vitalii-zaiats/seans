name: hls
description: Reads an HLS master playlist into the streams it offers. Knows nothing about HTTP.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.0

# Dependency-free: a master playlist is a dozen lines of text, and fetching it
# is the caller's job.
dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
