[project]
name = "httpkit"
version = "0.1.0"
description = "Shared httpx client: rotating proxies and retries on 429"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
