name: doh
description: Resolves names over HTTPS instead of the network's DNS. Knows nothing about HTTP.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.0

# Dependency-free: parsing is a small JSON shape, and performing the request is
# the caller's job (see DohFetcher).
dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
